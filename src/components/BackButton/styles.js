import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	wrap: {
		flexDirection: 'row',
		alignItems: 'center',
	},
	icon: {
		marginRight: 12,
		height: 30,
		width: 30,
	},
})

export default styles
