import { StyleSheet } from 'react-native'

import { variables } from 'styles'

const styles = StyleSheet.create({
	wrap: {
		marginLeft: -variables.layout.paddingHorizontal,
		marginRight: -variables.layout.paddingHorizontal,
		paddingLeft: 0,
		paddingRight: 0,
	},
})

export default styles
