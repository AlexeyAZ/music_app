import { fontFamilies, fontSizes } from '../../styles'

const defaultStyles = {
	flexGrow: 1,
	fontFamily: `${fontFamilies.gotham}-Regular`,
	fontSize: fontSizes.xs,
	height: '100%',
}

export default defaultStyles
