import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	wrap: {
		flexDirection: 'row',
		alignItems: 'center',
		flex: 1,
	},
	container: {
		flexDirection: 'row',
		alignItems: 'center',
		flex: 1,
	},
})

export default styles
